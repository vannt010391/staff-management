"""
WebSocket URL routing configuration for Django Channels
"""

from django.urls import path

websocket_urlpatterns = [
    # WebSocket routes will be added here
    # Example: path('ws/notifications/', NotificationConsumer.as_asgi()),
]
